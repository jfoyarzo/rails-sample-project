databases:
  - name: rails_sample_project
    databaseName: rails_sample_project
    user: rails_sample_project

services:
  - type: web
    name: rails_sample_project
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysite
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
